import os
import argparse

parser = argparse.ArgumentParser(description='')
parser.add_argument('-file', type=str, help='', default='heron.turtle')
parser.add_argument('-mode', choices=['weak', 'agressive'], help='Mode :)', default="agressive")

def main():
    pass

if __name__ == '__main__':
    main()
